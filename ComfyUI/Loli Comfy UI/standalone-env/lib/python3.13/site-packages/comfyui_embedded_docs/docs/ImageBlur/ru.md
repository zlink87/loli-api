> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/ImageBlur/ru.md)

Узел `ImageBlur` применяет гауссово размытие к изображению, позволяя смягчить края и уменьшить детализацию и шум. Он предоставляет контроль над интенсивностью и распространением размытия через параметры.

## Входы

| Поле          | Тип данных | Описание                                                                   |
|----------------|-------------|-------------------------------------------------------------------------------|
| `изображение`        | `IMAGE`     | Входное изображение для размытия. Это основной объект для применения эффекта размытия. |
| `радиус_размытия`  | `INT`       | Определяет радиус эффекта размытия. Больший радиус приводит к более выраженному размытию. |
| `сигма`        | `FLOAT`     | Управляет распространением размытия. Более высокое значение sigma означает, что размытие будет затрагивать более широкую область вокруг каждого пикселя. |

## Выходы

| Поле | Тип данных | Описание                                                              |
|-------|-------------|--------------------------------------------------------------------------|
| `изображение`| `IMAGE`     | На выходе получается размытая версия входного изображения, где степень размытия определяется входными параметрами. |
