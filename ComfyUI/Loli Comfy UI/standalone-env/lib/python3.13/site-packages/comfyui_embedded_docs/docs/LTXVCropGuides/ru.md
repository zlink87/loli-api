> Эта документация была создана с помощью ИИ. Если вы обнаружите ошибки или у вас есть предложения по улучшению, пожалуйста, внесите свой вклад! [Редактировать на GitHub](https://github.com/Comfy-Org/embedded-docs/blob/main/comfyui_embedded_docs/docs/LTXVCropGuides/ru.md)

Узел LTXVCropGuides обрабатывает кондиционирующие и латентные входные данные для генерации видео, удаляя информацию о ключевых кадрах и корректируя размеры латентного представления. Он обрезает латентное изображение и маску шума, исключая секции ключевых кадров, одновременно очищая индексы ключевых кадров из позитивных и негативных кондиционирующих входных данных. Это подготавливает данные для рабочих процессов генерации видео, которые не требуют управления по ключевым кадрам.

## Входные параметры

| Параметр | Тип данных | Обязательный | Диапазон | Описание |
|-----------|-----------|----------|-------|-------------|
| `положительный` | CONDITIONING | Да | - | Позитивный кондиционирующий вход, содержащий информацию для управления генерацией |
| `отрицательный` | CONDITIONING | Да | - | Негативный кондиционирующий вход, содержащий информацию о том, чего следует избегать при генерации |
| `скрытое пространство` | LATENT | Да | - | Латентное представление, содержащее сэмплы изображений и данные маски шума |

## Выходные параметры

| Выходной параметр | Тип данных | Описание |
|-------------|-----------|-------------|
| `положительный` | CONDITIONING | Обработанный позитивный кондиционирующий вход с очищенными индексами ключевых кадров |
| `отрицательный` | CONDITIONING | Обработанный негативный кондиционирующий вход с очищенными индексами ключевых кадров |
| `скрытое пространство` | LATENT | Обрезанное латентное представление с корректированными сэмплами и маской шума |
