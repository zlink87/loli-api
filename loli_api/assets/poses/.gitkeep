# Pose reference images live here as pose_ref_<posetype>.png (16 flat PNG files).
# They are generated once via scripts/generate_pose_refs.py (after RunPod is live)
# and committed to the repo. Shipped per-request as base64 input.images[] entries.
